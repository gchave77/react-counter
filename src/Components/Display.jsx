import React from "react";

function Display(props) {
  //   const [display, setDisplay] = useState(0);

  return (
    <div>
      <section className="display">This is my Display for the Counter.</section>
    </div>
  );
}

export default Display;
